# UI Kit — Book Interior

Interactive page-through of the Record's interior pages, copy set **exactly** as written in `CONTENT/` (and `EDITIONS/the-common-record/content.md` for the co-signed daily):

Identity Standard → Non-Negotiable Standards → Initiation Day 1 → Daily Page (×90 master) → Weekly Escalation → Close-Out, plus The Common Record daily (two signatures, never one).

Rules honored: single typeface (IBM Plex Sans), ruled lines not boxes, KEPT/BROKEN ▢ as the only marked field, Day/Week count as the only numbering, no decorative elements.

- `index.html` — entry; renders `Interior.jsx` (page tabs + prev/next).
