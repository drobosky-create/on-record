Ruled write-in field — the web version of the book's ruled line; use for every form input.

```jsx
<Field label="Name" />
<Field label="Email" placeholder="" />
<Field label="The record" multiline />
```

A single hairline underline, no box, no fill. Mono uppercase label. Focus turns the rule oxblood. `dark` for ink surfaces (bone rule, silver label).
