Uppercase mono label — the brand's "ledger" register; use wherever an eyebrow, counter, or field label is needed.

```jsx
<Label>Day 001 / 090</Label>
<Label dark>On Record · Design System</Label>
```

12px IBM Plex Mono, uppercase, letter-spacing .12em. Muted `#6E685F` on bone; `dark` switches to silver for ink surfaces. Separator is the middle dot `·`.
